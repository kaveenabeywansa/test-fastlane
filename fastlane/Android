def androidAppSign(bundleId="io.ionic.starter")
    puts "Android app (#{bundleId}) signing..."
    puts "PWD"
    sh "pwd"
    puts "Proj Workspace: #{ENV['PROJ_WORKSPACE']}"

    Dir.chdir("#{ENV['PROJ_WORKSPACE']}"){
        sh "pwd"
    }

    sh "pwd"
end